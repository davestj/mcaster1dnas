# -----------------------------------------------------------------------
# Compiler / linker intermediates
# -----------------------------------------------------------------------
.deps/
.libs/
*.la
*.lo
*.o
*.exe
*~
core

# -----------------------------------------------------------------------
# Autotools: files generated by ./configure  (NOT by autoreconf/automake)
# Makefile.am and Makefile.in are SOURCE and should stay tracked.
# Makefile (no extension) is generated by ./configure and should NOT.
# -----------------------------------------------------------------------
/autom4te.cache/
/config.h
/config.h.in
/config.log
/config.status
/git_hash.h
/libtool
/stamp-h1
/config.sub
# Exact match on "Makefile" — does NOT affect Makefile.am or Makefile.in
Makefile

# Autoconf/libtool-generated m4 macros (created by autoreconf)
/m4/libtool.m4
/m4/ltoptions.m4
/m4/ltsugar.m4
/m4/ltversion.m4
/m4/lt~obsolete.m4

# -----------------------------------------------------------------------
# Build output  (make / make install artifacts)
# -----------------------------------------------------------------------
build/
/src/mcaster1

# -----------------------------------------------------------------------
# SSL certificates and private keys  — NEVER commit secrets
# -----------------------------------------------------------------------
ssl/
*.pem
*.crt
*.key

# -----------------------------------------------------------------------
# Runtime logs (generated at run time, not source)
# -----------------------------------------------------------------------
/logs/
build/logs/

# -----------------------------------------------------------------------
# Production / deployment configs  (contain passwords and server details)
# -----------------------------------------------------------------------
mcaster1-production.yaml
mcaster1-production.xml
/conf/mcaster1.xml.dist
/conf/mcaster1_minimal.xml.dist
/conf/mcaster1_shoutcast_compat.xml.dist

# -----------------------------------------------------------------------
# Test / CI artifacts
# -----------------------------------------------------------------------
test-logs/
test-*.yaml
ci-test-*
YAML_TEST_REPORT.txt
install-root/
*.deb
*.rpm
*.tar.gz
*.tar.bz2

# -----------------------------------------------------------------------
# Claude Code session data (local AI assistant state — not source code)
# -----------------------------------------------------------------------
.claude/

# -----------------------------------------------------------------------
# Windows — Visual Studio build output directories
# These are generated by MSBuild / VS2022 and must never be committed.
# -----------------------------------------------------------------------

# Primary x64 build output trees (Debug and Release)
windows/x64/
windows/Win32/

# Per-project intermediate object / dependency dirs (all config/platform combos)
windows/Debug/
windows/Release/
windows/x64Debug/
windows/x64Release/

# MSBuild intermediate files
windows/**/*.obj
windows/**/*.pch
windows/**/*.pdb
windows/**/*.ilk
windows/**/*.idb
windows/**/*.res
windows/**/*.exp
windows/**/*.lib
windows/**/*.recipe
windows/**/*.FileListAbsolute.txt

# MSBuild transaction logs
windows/**/*.tlog
windows/**/Mcaster1Console.tlog
windows/**/Mcaster1Win.tlog
windows/**/Mcaster1Win.log
mcaster1-development-win.yaml
start-dev-win.bat
start-dev-win.ps1
windows/Archive/
windows/ResizableLib-repo/

# Generated headers (stamped at build time by PreBuildEvent)
windows/src/git_hash.h
src/git_hash.h

# Windows build diagnostic logs (written at runtime by CFG_TRACE / YAML_TRACE)
windows/mcaster1win_start.log
windows/**/mcaster1win_start.log

# Runtime pid and lock files
windows/**/*.pid
*.pid

# Windows installer build output
windows/*.exe
windows/Output/
windows/Setup/

# Visual Studio user-specific settings (not project config)
windows/**/*.user
windows/**/*.suo
windows/**/.vs/

# Compiled resource files
windows/**/*.aps

# Legacy VC6 workspace/project output (kept for reference; binaries excluded)
windows/**/*.opt
windows/**/*.ncb
windows/**/*.bak
